import './Logo.css'

/** Text wordmark. Swap for the supplied logo file when available. */
export default function Logo() {
  return (
    <span className="logo">
      <span className="logo__name">Giwagate</span>
      <span className="logo__sub">Properties</span>
    </span>
  )
}
